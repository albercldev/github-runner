name: github-actions
services:
  runner:
    image: ghcr.io/albercldev/github-runner:latest
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
